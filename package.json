{
  "name": "@safricloud/cl-workflow",
  "version": "0.6.0",
  "private": false,
  "description": "The contribution kit: a Claude Code process, its hooks and its installer.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Safricloud/cl-workflow.git"
  },
  "type": "module",
  "bin": {
    "cl-workflow": "dist/cli.js"
  },
  "files": [
    "dist",
    "template"
  ],
  "engines": {
    "node": ">=24"
  },
  "packageManager": "pnpm@10.27.0",
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "build": "tsc -p tsconfig.build.json",
    "lint": "eslint --max-warnings 0 .",
    "typecheck": "tsc --noEmit",
    "selftest": "node template/.claude/hooks/rule-zero-selftest.ts",
    "test": "node --test \"test/**/*.test.ts\""
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^24.13.3",
    "eslint": "^10.9.1",
    "eslint-plugin-n": "^18.3.0",
    "prettier": "^3.9.6",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.68.0"
  }
}
