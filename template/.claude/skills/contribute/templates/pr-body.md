<One paragraph: the ask, its source (issue #n / conversation, <date>), the direction taken, the
outcome.>

## What changed
- <bullet per plan item, user-visible behaviour first, mechanism second>

## Decisions to veto
- **At the Questions phase:** <n> decisions recorded in `docs/history/<id>/review.md`
- **Made by the plan:** <each, one line, with grounds>
- **Made mid-loop:** <each, one line, with grounds — implemented already>

## Validation
- <formatter check clean>; <full check with counts>; <E2E suites and results>; <new tests seen
  to fail against a pre-change build>; <screenshots appraised at which viewports>; <container
  run if touched>
- Known flake / not run: <say it>

## Records
Review, investigations and plan (`plan.md`, `phase-*.md`) archived in `docs/history/<id>/`.
Ledger: <what changed in `mem/outstanding.md`>. Rule-zero actions: <from the log, or none>.

## Blocked on the owner
- #<n> — <title> (`blocked-on-owner`, `<kind>`, `<area>`) — or none

<!-- Nothing in the repo is updated after this PR opens. Review cycles are recorded as PR
comments; merge and deploy are reported in the conversation. -->

Fixes #<n>
